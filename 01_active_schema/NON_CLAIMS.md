# Active_Schema V1 Non-Claims and Scope Boundary

```yaml
schema_version: "1.0.0"
document_role: NON_CLAIMS
```

Active_Schema V1 does not claim the following.

## 1. Prior Formation Is Not the New Canonical Root

```text
Active_Schema V1
≠
before_position integration
≠
NbyN full canon
≠
Y_Branch full canon
≠
dot.meta.md revision
```

Prior material remains historical formation and selective evidence. It is not automatically inherited.

## 2. No Universal Physical or Historical Claim

Past analogies involving physics, geometry, language, history, pyramids, fields, light, gravity, time, or dimensions are not promoted into universal law by this package.

```text
structural similarity
≠
scientific identity
```

## 3. No Global Center File

`Hash Relation Track DB` is not a replacement `dot.meta.md` and does not require one file to contain all meaning.

## 4. Independent Identities Are Not Equal

```text
DOI
≠
ZIP SHA-256
≠
Manifest Digest
≠
Git Commit
```

The identities are bound without being merged.

## 5. Hash Is Not Meaning or Truth

```text
Hash
≠
Truth
≠
Meaning
≠
Semantic Identity
≠
Position
```

## 6. Relation Repo Is Not the Whole Distributed DB

```text
Relation Repo
≠
Hash Relation Track DB
```

The new registry is an append-only extension inside the existing integrated Relation repository.

## 7. for_instance Is Not Only a Seat Definition Store

The existing repository contains Alignment, Assignment, History, Capability, Handoff, Operation, and Test structures.

The new binding does not erase or redefine that existing repository identity.

## 8. Existing Assignment History Is Not New Seat Occupation

Existing objects in `for_instance/04_assignment/` remain at their existing position.

They are not automatically migrated into `SEAT_OCCUPATION` records.

## 9. No Fixed Runtime Population

```text
Seat Grammar
≠
Fixed Runtime Population
```

The coordinate view `x/y/z/xz/yz/xy/xyz/t` is optional and does not define a mandatory instance count.

## 10. No Private Chain-of-Thought Archive

The package does not require or store private chain-of-thought.

It stores external results, declared summaries, evidence, states, and Relations.

## 11. No Future Reference or Self-Containing Commit

A Record does not predeclare an unknown future event.

An Extension Origin does not reference a Genesis Binding Record that does not yet exist.

A Record does not contain the unknown hash of the Commit that will contain the same Record.

## 12. No Mutable Rewrite of Append-Only History

Declared states are immutable.

Effective states are derived.

Historical bytes are not silently edited to represent later state.

## 13. C Is Not Canonical Registry

```text
C
≠
Source of Truth

C
=
Observable Projection
```

A Closure C Snapshot is evidence and does not replace its sources.

## 14. ZIP SHA-256 Placement

The final ZIP SHA-256 exists only as the ZIP filename:

```text
<64-lowercase-sha256>.zip
```

There is no prefix, hash sidecar, or internal ZIP self-hash value.

## 15. V1 Does Not Complete Every Runtime Feature

V1 does not claim completion of the full query engine, all runtime automation, all external field validation, all multi-instance testing, all future lineages, or every implementation API.

## 16. Formation Provenance Is Not Permanent Authority

Current instance names may appear as provenance.

They do not define permanent roles or grant mutation authority.

## 17. Crystallized Package Is Not Publication Approval

This Round 10 object is content-frozen by `gpt.direct` and awaits `gpt.logi` final review.

```text
Final Review PASS
→ same frozen ZIP may be authorized for Zenodo

Final Review HOLD or FAIL
→ frozen ZIP is not published
→ a new crystallized object is required
```
