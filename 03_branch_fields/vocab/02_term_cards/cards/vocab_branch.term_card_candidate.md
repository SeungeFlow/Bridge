# Term card candidate — vocab.branch

## status

term: vocab.branch
filename_slug: vocab_branch
card_status: CANDIDATE
final_definition: false
source_scope: vocab.branch MVP / README baseline / SQLite reader / first term-card layer
created_from: vocab.branch MVP milestone and term-card candidate sequence
created_in_pass: 66
base_commit_repo_9dot0: b1dee861f44a5dd18d787185a4a53205eb9e8681

## 1. surface

```text
vocab.branch
```

## 2. core sentence

```text
vocab.branch는 단어장이 아니라, 생각의 표면을 구조적으로 해체하는 첫 branch.
```

Candidate reading:

```text
vocab.branch is not a vocabulary notebook.
vocab.branch is the first branch-field that structurally decomposes the surface of thought.
```

This card does not finalize vocab.branch.

This card records vocab.branch as a controlled candidate for the first vocabulary / source-memory / AI-reader branch surface in Repo.9Dot0.

## 3. candidate role

vocab.branch is a branch-field candidate that connects:

```text
word
Raw URL
source identity
README baseline
Seed.Base
Y_Branch
Ctp / Ctp24
AI DB reader
term-card candidates
```

It turns word surfaces into structured source-linked triggers.

It does not replace source.

It does not finalize interpretation.

## 4. MVP chain

The current MVP chain is:

```text
vocab.branch skeleton
→ README vocab source list
→ README vocab scan protocol
→ v1 raw preview
→ v2 structural filter
→ v3 core-token recovery
→ SQLite MVP schema
→ local runtime SQLite DB build
→ readme_source metadata correction
→ AI DB reader read-only query script
→ read-only smoke test
→ AI DB reader handoff prompt
→ first term-card candidates
```

## 5. DB status

```text
status: branch_field_candidate
score: not_assigned_in_pass66
occurrence_count: not_queried_in_pass66
source_count: not_queried_in_pass66
```

Reason:

```text
PASS66 does not run query_vocab_reader.py.
This card is created from the committed vocab.branch MVP milestone and first term-card candidate sequence.
Exact DB occurrence counts and Raw URL anchors should be added after a later read-only DB query.
```

Known MVP DB state:

```text
source_file=8
readme_source=8
vocab_term=1710
term_status=1710
term_occurrence=3855
raw_url_index=3855
core_token_candidate=162
structural_trigger_candidate=403
```

## 6. README baseline presence

vocab.branch is present as a Repo.9Dot0 branch-field surface.

Current readme_source baseline rows include:

```text
1|repo_9dot0_branch_field|SeungeFlow/9Dot0|main|README.md
2|repo_9dot0_branch_field|SeungeFlow/9Dot0|main|03_branch_fields/market/README.md
3|repo_9dot0_branch_field|SeungeFlow/9Dot0|main|03_branch_fields/sohosa/README.md
4|repo_9dot0_branch_field|SeungeFlow/9Dot0|main|03_branch_fields/history/README.md
5|repo_9dot0_branch_field|SeungeFlow/9Dot0|main|03_branch_fields/vocab/README.md
6|repo_seungeflow_primary_observer|SeungeFlow/SeungeFlow|Y_Branch|README.md
7|repo_seungeflow_primary_observer|SeungeFlow/SeungeFlow|main|README.md
8|repo_seungeflow_primary_observer|SeungeFlow/SeungeFlow|first_flow|README.md
```

## 7. source identity requirement

Every useful vocab.branch output must preserve:

```text
repo | branch | commit | path | line_no | raw_url | context
```

vocab.branch must not produce unsupported vocabulary expansion.

If a needed structural term is missing from the relevant README.md, update the README.md first.

## 8. compact source context

### Surface decomposition

```text
생각의 표면을 구조적으로 해체하는 첫 branch
```

Candidate reading:

```text
The first target surface is not an abstract idea.
The first readable surface is word / text / README / md source.
```

### Minimal connector

```text
word + Raw URL
```

Candidate reading:

```text
word triggers meaning.
Raw URL fixes source identity.
```

### DB reader

```text
The DB is a derived Seed.Base-like reader field.
```

Candidate reading:

```text
vocab.branch uses DB only as a reader surface, not as original source.
```

## 9. structural role candidate

```text
branch_field_candidate
vocabulary_law_surface
thought_surface_decomposition_field
word_trigger_field
source_memory_reader_field
README_baseline_dependent_branch
Raw_URL_source_guard_branch
AI_DB_reader_branch
term_card_generation_branch
```

## 10. relation candidates

```text
vocab.branch ↔ word
vocab.branch ↔ Raw URL
vocab.branch ↔ source identity
vocab.branch ↔ README baseline
vocab.branch ↔ Seed.Base
vocab.branch ↔ Y_Branch
vocab.branch ↔ Ctp
vocab.branch ↔ Ctp24
vocab.branch ↔ AI DB reader
vocab.branch ↔ term-card candidate
vocab.branch ↔ branch.market
vocab.branch ↔ branch.sohosa
vocab.branch ↔ branch.history
```

## 11. first completed term-card sequence

Candidate cards already created or committed in the first sequence:

```text
C
m
t
p
?
Ctp
Ctp24
Raw URL
source identity
README baseline
Seed.Base
Y_Branch
```

vocab.branch is the self-reference card that records the branch-field holding this sequence.

## 12. branch support candidates

### branch.market

Candidate only:

```text
vocab.branch may support branch.market by controlling the vocabulary and source identity before Price is mapped as C.
```

Possible later use:

```text
Price as C
m-elements forming Price
t-transition/execution flow
p-runtime/MT5 field
? unresolved execution condition
```

### branch.sohosa

Candidate only:

```text
vocab.branch may support branch.sohosa by controlling sohosa/hyangsa vocabulary and source identity before ritual-lineage source body is interpreted as C.
```

Possible later use:

```text
소호사
향사
제례
계보
장소
사료
source identity
Raw URL
```

### branch.history

Candidate only:

```text
vocab.branch may support branch.history by controlling origin/history/etymology vocabulary before historical meaning is interpreted as C.
```

Possible later use:

```text
origin
etymology
transformation
time-layer
source transmission
historical field
```

## 13. what this card does not claim

This card does not claim:

- final definition of vocab.branch
- final completion of vocab.branch
- full source coverage
- whole-repository scan completion
- final term-card completion
- final relation judgment
- final market alignment
- final sohosa alignment
- final history alignment
- final proof of any structure principle

## 14. next action

Possible next actions:

```text
1. commit this vocab.branch term-card candidate
2. create first term-card index report
3. run read-only DB query for each created card and add exact Raw URL anchors later
4. prepare branch.market vocabulary baseline alignment
5. prepare branch.sohosa vocabulary baseline alignment
6. prepare branch.history vocabulary baseline alignment
7. create guard card candidate
8. create relation card candidate
```

## 15. guard

This card is a candidate.

This card is not a final definition.

vocab.branch is not a word list.

vocab.branch is not complete.

DB output is derived evidence.

Original source remains in md files and Raw URLs.

gpt.direct performs final structure alignment.
